<UserControl x:Class="Saki.Framework.DesktopApp.SakiTreeUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Saki.Framework.DesktopApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <local:TreeItemDataTemplateSelector x:Key="TreeItemTemplateSelector"></local:TreeItemDataTemplateSelector>
    </UserControl.Resources>
    
    <Grid DataContext="{Binding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <TreeView ItemsSource="{Binding Items}" ItemTemplateSelector="{StaticResource TreeItemTemplateSelector}">

            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                    <Setter Property="ItemsSource" Value="{Binding Items}"></Setter>
                    <Setter Property="ToolTip" Value="{Binding Description}"></Setter>
                </Style>
            </TreeView.ItemContainerStyle>
            
        </TreeView>
        
    </Grid>
    
</UserControl>
